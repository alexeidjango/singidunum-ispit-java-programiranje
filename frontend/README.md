# Dev Setup

1. Edit `.env` file and change the value of `VITE_API_BASE_URL` to match the backend API server
2. Install packages by running `npm install`
3. Run dev server from terminal `npm run dev`

